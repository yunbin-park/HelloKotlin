<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.androidapp.hellokotlin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HelloKotlin">
        <activity android:name=".BmiKotlinActivity"></activity>
        <activity
            android:name=".BmiJavaActivity"
            android:label="BmiJavaActivity" />
        <activity
            android:name=".MainActivity"
            android:label="BMI calculation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".BmiKotlinActivity"
            android:label="BmiKotlinActivity" />
    </application>

</manifest>